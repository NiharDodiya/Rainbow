import 'package:flutter/material.dart';

class AdPaymenetScreen extends StatelessWidget {
  const AdPaymenetScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
